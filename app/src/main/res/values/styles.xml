<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="FiveStarsStyle1" parent="@android:style/Widget.Holo.RatingBar.Small">
        <item name="android:minHeight">30dp</item>
        <item name="android:maxHeight">30dp</item>
    </style>


    <!--控制dialog进出动画-->

    <style name="dialog_animation" parent="android:Animation">

        <item name="android:windowEnterAnimation">@animator/dialog_enter</item>

        <!-- 进入时的动画 -->

        <item name="android:windowExitAnimation">@animator/dialog_exit</item>

        <!-- 退出时的动画 -->

    </style>

    <!--控制dialog进出动画-->

    <style name="course_list_selection_animation" parent="android:Animation">

        <item name="android:windowEnterAnimation">@animator/course_list_selection_enter</item>

        <!-- 进入时的动画 -->

        <item name="android:windowExitAnimation">@animator/course_list_selection_exit</item>

        <!-- 退出时的动画 -->

    </style>

    <style name="live_outline_animation" parent="android:Animation">

        <item name="android:windowEnterAnimation">@animator/live_outline_enter</item>

        <!-- 进入时的动画 -->

        <item name="android:windowExitAnimation">@animator/live_outline_exit</item>

        <!-- 退出时的动画 -->

    </style>

    <style name="evaluate_animation" parent="android:Animation">

        <item name="android:windowEnterAnimation">@animator/evaluate_enter</item>

        <!-- 进入时的动画 -->

        <item name="android:windowExitAnimation">@animator/evaluate_exit</item>

        <!-- 退出时的动画 -->

    </style>

    <style name="MyDialogStyleBottom" parent="android:Theme.Dialog">
        <item name="android:windowAnimationStyle">@style/dialog_animation</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

</resources>
